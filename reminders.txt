- When writing code, always put the language. This has to be done to make sure the code will overflow correctly on an html page. i.e., do this:

```bash
bash command
```

not this:

```
bash command
```

/Users/BenRombaut/opt/anaconda3/envs/articles/bin/jupyter nbconvert ./classify_mnist_hello_ml.ipynb --to markdown